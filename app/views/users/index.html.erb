 <div class="Navigation">
          <div class="Navigation-Left">
            <%= image_tag("logo.jpg",class: "Logo-icon") %>
            <%= image_tag("name.png",class: "Logo-name" )%>
            <%= link_to "Home", new_menu_items_home_path%>
          </div>
          <div class="Navigation-Right">
            <%= link_to "Orders", orders_path%>
            <%= link_to "Menus", menus_path%>
            <%= link_to "Users", users_path%>
            <%= link_to "Sign-out", destroy_session_path,method: "delete" %>
          </div>
</div>
<h2>Billing Clerk:<h2>
<ul>
  <% User.clerk.each do |clerk| %>
    <li class="User">
      <div class="User-container">
        <p class = "User-name"><%=clerk.name%></p>
        <a class="User-delete ">
                <%= form_with(url: "/users/#{clerk.id}" , method: "delete") do %>
                  <%= image_submit_tag("trash.svg") %>
                <% end %>
        </a>
      </div>
    </li>
  <% end %>
</ul>
<div class="Users">
<h3>Add User:<%= link_to "Add-User", new_user_path%></h3>
</div>
